#include <bits/stdc++.h>
using namespace std;

// 前缀优化一下就好


signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n, m, k; cin >> n >> m >> k;

    vector<string> g(n + 1);
    for (int i = 1; i <= n; i ++ ) cin >> g[i], g[i] = " " + g[i];

    
}