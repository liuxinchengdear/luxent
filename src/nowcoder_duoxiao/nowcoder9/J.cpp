#include <bits/stdc++.h>
using namespace std;

#define MULTI int _T; cin >> _T; cin.ignore(); while (_T -- )

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    MULTI {
        string s;
        getline(cin, s);
        cout << s << "nya\n";
    }
    return 0;
}