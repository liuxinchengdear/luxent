#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    long long res  = 0;

    int q; cin >> q;
    while (q -- ) {
        int n, m; cin >> n >> m;
        cout << 1LL * n * m << endl;
    }
    return 0;
}