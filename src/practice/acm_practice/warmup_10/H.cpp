#include <bits/stdc++.h>
using namespace std;

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    double x; cin >> x;
    cout << "Vivo " << fixed << setprecision(3) << (0.5 * x + 50) << endl;
    return 0;
}