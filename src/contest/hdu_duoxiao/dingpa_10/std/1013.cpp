#include<bits/stdc++.h>
using namespace std;
signed main(){
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    long long T, n, m;
    cin >> T;
    while(T--){
        cin >> n >> m;
        cout << n * m << '\n';
    }
    return 0;
}