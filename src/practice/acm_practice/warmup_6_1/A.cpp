#include <bits/stdc++.h>
using namespace std;

#define int long long

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int a, b, c, d; cin >> a >> b >> c >> d;
    cout << (1ll * (a + b + c) * d) << endl;
    return 0;
}