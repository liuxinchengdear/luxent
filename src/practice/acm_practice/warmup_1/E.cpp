// #include <bits/stdc++.h>
// using namespace std;

// int main() {
//     ios::sync_with_stdio(false);
//     cin.tie(0);

//     int _T; cin >> _T; while (_T -- ) {
//         int n, m; cin >> n >> m;
//         if (min(n, m) == 1) {
//             if (max(n, m) % 4) cout << "NO\n";
//             else {
//                 for (int i = 0; i < n; i ++ )
//                     for (int j = 0; j < m; j ++ ) cout << (i * m + j) / 4 + 1 << " \n"[j == m - 1];
//             }
//         }
//         else if (min(n, m) == 2) {
//             if (max(n, m) % 4 && max(n, m) % 6)
//         }
//     }
// }